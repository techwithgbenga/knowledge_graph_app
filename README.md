# knowledge_graph_app
An application that lets you ingest documents (PDFs, Markdown, text), build a semantic knowledge graph (entities + relations), and expose a Streamlit-based semantic search UI backed by a vector store (FAISS) and OpenAI embeddings.
